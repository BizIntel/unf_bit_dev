
#Область ПрограммныйИнтерфейс

// Процедура записывает накопленную статистику для отправки подсистемой "Центр мониторинга".
// Накопленная статистика очищается.
//
// Параметры:
//  НакопленнаяСтатистика	 - СписокЗначений	 - список имен бизнес-операций. После записи, параметр очищается.
//
Процедура ЗаписатьСтатистику(НакопленнаяСтатистика) Экспорт
	
	Если ТипЗнч(НакопленнаяСтатистика) <> Тип("СписокЗначений") Тогда
		Возврат;
	КонецЕсли;
	
	Если НакопленнаяСтатистика.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого ЭлементСписка Из НакопленнаяСтатистика Цикл
		
		ЦентрМониторинга.ЗаписатьОперациюБизнесСтатистики(
			ЭлементСписка.Значение,
			1
		);
		
	КонецЦикла;
	
	НакопленнаяСтатистика.Очистить();
	
КонецПроцедуры

#КонецОбласти