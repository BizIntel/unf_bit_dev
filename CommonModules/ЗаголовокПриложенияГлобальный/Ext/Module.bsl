
#Область ПрограммныйИнтерфейс

// Процедура - обработчик оповещения, выполняет действия при изменении данных, влияющих на заголовок клиентского приложения
//
// Параметры:
//  см. Оповестить() в синтакс-помощнике
//
Процедура ИзменениеДанныхДляЗаголовкаПриложенияОбработкаОповещения(ИмяСобытия, Параметр, Источник) Экспорт
	
	Если ИмяСобытия = "Запись_КурсыВалют"
		Или ИмяСобытия = "Запись_ЗагрузкаКурсовВалют"
		Или Источник = "ФункциональнаяУчетВалютныхОпераций"
		Или (ИмяСобытия = "Запись_НаборКонстант" И Источник = "ЗаголовокСистемы") Тогда
		
		СтандартныеПодсистемыКлиент.УстановитьРасширенныйЗаголовокПриложения();
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
