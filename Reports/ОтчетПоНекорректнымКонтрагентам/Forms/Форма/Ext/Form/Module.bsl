
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АдресТабДок") Тогда
		ТабДок = ПолучитьИзВременногоХранилища(Параметры.АдресТабДок);
	ИначеЕсли Параметры.Свойство("ТабДок") Тогда
		ТабДок = Параметры.ТабДок;
	Иначе
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	ПредставлениеОтчета = Параметры.ПредставлениеОтчета;
	
	Заголовок = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Результаты проверки контрагентов сервисом ФНС (%1)'"),
			ПредставлениеОтчета);
	
	// Добавляем в заголовок ссылку на инструкцию.
	// Не добавляем ее на форме, так как ссылка переопределяемая.
	Элементы.ПояснениеКритичностиОшибок.Заголовок = 
		Новый ФорматированнаяСтрока(
			Элементы.ПояснениеКритичностиОшибок.Заголовок,
			ПроверкаКонтрагентовБРО.СсылкаНаИнструкцию());
			
	ТабличныйДокумент.Очистить();
	ТабличныйДокумент.Вывести(ТабДок);
	
КонецПроцедуры

#КонецОбласти
