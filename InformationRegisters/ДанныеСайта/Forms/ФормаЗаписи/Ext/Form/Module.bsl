
&НаСервере
Процедура ПослеЗаписиНаСервере(ТекущийОбъект, ПараметрыЗаписи)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ДанныеСайта.Организация,
	|	ДанныеСайта.АдресСайта,
	|	ДанныеСайта.IDСайта,
	|	ДанныеСайта.EMail,
	|	ДанныеСайта.ПарольСайта,
	|	ДанныеСайта.ТипСайта,
	|	ДанныеСайта.URLАдминЗоны,
	|	ДанныеСайта.СайтСоздан
	|ИЗ
	|	РегистрСведений.ДанныеСайта КАК ДанныеСайта
	|ГДЕ
	|	ДанныеСайта.СайтСоздан";
	
	Результат = Запрос.Выполнить();
	Если НЕ Результат.Пустой() Тогда
		ПараметрыЗаписи.Вставить("ОбновитьИнтерфейс", Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
	
	Если ПараметрыЗаписи.Свойство("ОбновитьИнтерфейс") Тогда
		ОбновитьИнтерфейс();
	КонецЕсли;
	
КонецПроцедуры
