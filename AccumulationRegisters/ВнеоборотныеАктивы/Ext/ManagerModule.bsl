#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда  	
		Возврат;  		
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	ВнеоборотныеАктивы.НомерСтроки КАК НомерСтроки,
	|	ВнеоборотныеАктивы.Организация КАК Организация,
	|	ВнеоборотныеАктивы.ВнеоборотныйАктив КАК ВнеоборотныйАктив,
	|	ВнеоборотныеАктивы.Стоимость КАК СтоимостьПередЗаписью,
	|	ВнеоборотныеАктивы.Стоимость КАК СтоимостьИзменение,
	|	ВнеоборотныеАктивы.Стоимость КАК СтоимостьПриЗаписи,
	|	ВнеоборотныеАктивы.Амортизация КАК АмортизацияПередЗаписью,
	|	ВнеоборотныеАктивы.Амортизация КАК АмортизацияИзменение,
	|	ВнеоборотныеАктивы.Амортизация КАК АмортизацияПриЗаписи
	|ПОМЕСТИТЬ ДвиженияВнеоборотныеАктивыИзменение
	|ИЗ
	|	РегистрНакопления.ВнеоборотныеАктивы КАК ВнеоборотныеАктивы");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияВнеоборотныеАктивыИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#КонецОбласти

#КонецЕсли