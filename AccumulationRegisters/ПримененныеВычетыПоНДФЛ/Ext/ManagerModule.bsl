#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда	
		Возврат;		
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	ПримененныеВычетыПоНДФЛ.НомерСтроки КАК НомерСтроки,
	|	ПримененныеВычетыПоНДФЛ.Организация КАК Организация,
	|	ПримененныеВычетыПоНДФЛ.Сотрудник КАК Сотрудник,
	|	ПримененныеВычетыПоНДФЛ.ПериодРегистрации КАК ПериодРегистрации,
	|	ПримененныеВычетыПоНДФЛ.КодВычета КАК КодВычета,
	|	ПримененныеВычетыПоНДФЛ.СуммаВычета КАК СуммаВычетаПередЗаписью,
	|	ПримененныеВычетыПоНДФЛ.СуммаВычета КАК СуммаВычетаИзменение,
	|	ПримененныеВычетыПоНДФЛ.СуммаВычета КАК СуммаВычетаПриЗаписи
	|ПОМЕСТИТЬ ДвиженияПримененныеВычетыПоНДФЛИзменение
	|ИЗ
	|	РегистрНакопления.ПримененныеВычетыПоНДФЛ КАК ПримененныеВычетыПоНДФЛ");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияПримененныеВычетыПоНДФЛИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#КонецОбласти

#КонецЕсли