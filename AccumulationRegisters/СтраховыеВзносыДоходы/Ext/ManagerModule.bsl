#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда	
		Возврат;		
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	СтраховыеВзносыДоходы.НомерСтроки КАК НомерСтроки,
	|	СтраховыеВзносыДоходы.Организация КАК Организация,
	|	СтраховыеВзносыДоходы.Сотрудник КАК Сотрудник,
	|	СтраховыеВзносыДоходы.ВидДохода КАК ВидДохода,
	|	СтраховыеВзносыДоходы.Сумма КАК СуммаПередЗаписью,
	|	СтраховыеВзносыДоходы.Сумма КАК СуммаИзменение,
	|	СтраховыеВзносыДоходы.Сумма КАК СуммаПриЗаписи,
	|	СтраховыеВзносыДоходы.Скидка КАК СкидкаПередЗаписью,
	|	СтраховыеВзносыДоходы.Скидка КАК СкидкаИзменение,
	|	СтраховыеВзносыДоходы.Скидка КАК СкидкаПриЗаписи
	|ПОМЕСТИТЬ ДвиженияСтраховыеВзносыДоходыИзменение
	|ИЗ
	|	РегистрНакопления.СтраховыеВзносыДоходы КАК СтраховыеВзносыДоходы");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияСтраховыеВзносыДоходыИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#КонецОбласти

#КонецЕсли