
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	Если ПараметрыВыполненияКоманды.Источник=Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ПараметрыВыполненияКоманды.Источник.ИмяФормы = "Справочник.Номенклатура.Форма.ФормаЭлемента" Тогда
		РаботаСНоменклатуройКлиент.ПодготовитьРеквизитыСтрокиПоЭлементу(ПараметрыВыполненияКоманды, "ЗаказПокупателя");
	Иначе
		РаботаСНоменклатуройКлиент.ПодготовитьРеквизитыСтрокСписка(ПараметрыВыполненияКоманды, "ЗаказПокупателя");
	КонецЕсли;
	
КонецПроцедуры
