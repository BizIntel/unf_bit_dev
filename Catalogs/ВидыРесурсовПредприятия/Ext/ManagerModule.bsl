#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ВидыРесурсовПредприятия.Ссылка КАК Ссылка,
	|	ВидыРесурсовПредприятия.Наименование КАК РесурсПредприятияНаименование,
	|	ВидыРесурсовПредприятия.Код КАК РесурсПредприятияКод
	|ИЗ
	|	Справочник.ВидыРесурсовПредприятия КАК ВидыРесурсовПредприятия
	|ГДЕ
	|	ВидыРесурсовПредприятия.Ссылка <> &ВсеРесурсы
	|
	|СГРУППИРОВАТЬ ПО
	|	ВидыРесурсовПредприятия.Ссылка,
	|	ВидыРесурсовПредприятия.Наименование,
	|	ВидыРесурсовПредприятия.Код
	|
	|ИМЕЮЩИЕ
	|	ПОДСТРОКА(ВидыРесурсовПредприятия.Наименование, 1, &ДлинаПодстроки) ПОДОБНО &СтрокаПоиска
	|
	|УПОРЯДОЧИТЬ ПО
	|	РесурсПредприятияНаименование";
	
	Запрос.УстановитьПараметр("ВсеРесурсы", Справочники.ВидыРесурсовПредприятия.ВсеРесурсы);
	Запрос.УстановитьПараметр("СтрокаПоиска", Параметры.СтрокаПоиска);
	Запрос.УстановитьПараметр("ДлинаПодстроки", СтрДлина(Параметры.СтрокаПоиска));
	
	Результат = Запрос.Выполнить();
	Если НЕ Результат.Пустой() Тогда
		ДанныеВыбора = Новый СписокЗначений;
		Выборка = Результат.Выбрать();
		Пока Выборка.Следующий() Цикл
			ПредставлениеВыбора = СокрЛП(Выборка.Ссылка) + " (" + СокрЛП(Выборка.РесурсПредприятияКод) + ")";
			ДанныеВыбора.Добавить(Выборка.Ссылка, ПредставлениеВыбора);
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли