
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОчиститьСообщения();
	
	Если ТипЗнч(ПараметрыВыполненияКоманды.Источник) = Тип("УправляемаяФорма") Тогда
		ПараметрыСинхронизации = Новый Структура;
		МассивНастроекОбмена = Новый Массив;
		МассивНастроекОбмена.Добавить(ПараметрыВыполненияКоманды.Источник.Объект.НастройкаОбмена);
		ПараметрыСинхронизации.Вставить("НастройкиОбмена", МассивНастроекОбмена);
		ПараметрыСинхронизации.Вставить("ТекущийИндексСинхронизацииСБанками", 0);
		ПараметрыСинхронизации.Вставить("ИтогКолОтправленных", 0);
		ПараметрыСинхронизации.Вставить("ИтогКолПолученных", 0);
		ОбменСБанкамиСлужебныйКлиент.ВыполнитьОбменСБанками(Неопределено, ПараметрыСинхронизации);
	Иначе
		ОбменСБанкамиКлиент.СинхронизироватьСБанком();
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти